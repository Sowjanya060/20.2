#include<stdio.h>
int SumEven(int x, int n)
{
   if(x>n)
    return 0;
return x+SumEven(x+2,n);
}
int main()
{
    int x=2,n;
    printf("Enter n:");
    scanf("%d",&n);
    printf("Sum of all Even numbers is: %d",SumEven(x,n));
}
